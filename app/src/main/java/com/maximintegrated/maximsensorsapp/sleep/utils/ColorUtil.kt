package com.maximintegrated.maximsensorsapp.sleep.utils

import android.graphics.Color

class ColorUtil {
    companion object {
        val COLOR_WAKE: Int = Color.rgb(241, 92, 34)
        val COLOR_REM: Int = Color.rgb(158, 195, 222)
        val COLOR_LIGHT: Int = Color.rgb(110, 37, 133)
        val COLOR_DEEP: Int = Color.rgb(255, 194, 32)
        val COLOR_CENTER_TEXT: Int = Color.rgb(125, 175, 86)
    }
}